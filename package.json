{
  "name": "family-flow",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "dev": "node --env-file=.env --watch --import tsx src/app/server.ts",
    "dev:oidc": "node --env-file=.env.dev --watch --import tsx src/app/server.ts",
    "build": "tsc -p tsconfig.json && mkdir -p dist/adapters/http/assets && cp src/adapters/http/assets/app.css dist/adapters/http/assets/app.css",
    "start": "node dist/app/server.js",
    "db:migrate": "tsx src/adapters/db/migrate.ts",
    "arch:check": "tsx scripts/check-architecture.ts",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "lint": "biome lint . && pnpm arch:check",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "dependencies": {
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.6.0",
    "htmx.org": "^2.0.10",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.5",
    "@playwright/test": "^1.54.2",
    "@types/node": "^24.1.0",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
